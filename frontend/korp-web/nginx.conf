server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Angular Router uses HTML5 pushState — unknown paths must fall back to
    # index.html so client-side routing can take over.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
